<div class="flex flex-col items-center h-screen bg-gradient-to-r from-emerald-400 to-emerald-200">
<div class=" mx-auto p-4 bg-gradient-to-r from-green-300 to-green-400 font-montserrat font-extrabold rounded-lg mt-52 w-1/2">
  <h1 class="text-4xl font-bold mb-4"><%= @book.name %></h1>
  <p class="text-xl text-gray-700 mb-6">Автор: <%= @book.author %></p>

  <h2 class="text-2xl mb-2 font-montserrat font-extrabold">Пользователи, взявшие книгу:</h2>
  <div class="bg-white shadow rounded-lg p-4 bg-gradient-to-r from-green-900 to-green-700">
    <table class="min-w-full divide-y divide-gray-200 ">
      <thead>
      <tr>
        <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-white uppercase tracking-wider">Имя пользователя</th>
        <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-white uppercase tracking-wider">Действие</th>
      </tr>
      </thead>
      <tbody class="bg-white divide-y divide-gray-200">
      <% @users.each do |user| %>
        <tr class="rounded-lg">
          <td class="px-6 py-4 whitespace-nowrap ">
            <div class="text-sm text-gray-900 "><%= user.username %></div>
          </td>
          <td class="px-6 py-4 whitespace-nowrap text-right text-sm font-medium ">
            <%= button_to 'Принять книгу', admin_dashboard_take_book_user_path(@book.id, user.id), method: :get, class: 'bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded transition-all duration-300 ease-in-out' %>
          </td>
        </tr>
      <% end %>
      </tbody>
    </table>
  </div>
</div>
</div>